To run the program
Use the images link in txt file and replace in code (stock images)
Change the password in the mysql connection to 'root'
The code block to download dlib's shape predictor is in string form. Convert it back to code.
change the password in the mysql connection to whatever the mysql password is on your computer.
The code doesn't run if you don't open mysql in the background first.
